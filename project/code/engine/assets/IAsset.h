#pragma once

#include <string>

///===================================================
/// 視覚 / 聴覚 で 感じられる 現象に
/// 変化する前の データ(texture,audio,model などのデータ)
/// を 自作エンジンで使える形にしたもの
///===================================================
struct IAsset{
public:
	IAsset(){}
	virtual ~IAsset(){}
};